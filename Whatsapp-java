package Whatsapp;

import java.util.Scanner;
import java.util.regex.Pattern;

public class Whatsapp {   
   
 
    private String registeredUsername;
    private String registeredPassword;
    private String registeredCellPhone;
    private String firstName;
    private String lastName;
    public Whatsapp() { 
        
    }
   
    public Whatsapp(String firstName, String lastName) {
        this.firstName = firstName;
        this.lastName = lastName;
       
    }
    public String getFirstName() {
        return firstName;
    }
    public void setFirstName(String firstName) {
        this.firstName = firstName;
    }
    public String getLastName () {
        return lastName;
    }
    public void setLastName (String lastName) {
            this.lastName = lastName;
            
    public static void main(String[] args) {
                Scanner scanner = new Scanner(System.in);
                System.out.println("===USER REGISTRATION");
                System.out.println("Enter First Name: ");
                String firstName = scanner.nextLine();
                
                System.out.println("Enter Last Name: ");
                String lastName = scanner.nextLine();
                
                Whatsapp userLogin = new Whatsapp(firstName, lastName);
                
                System.out.print("Enter username");
                String username= scanner.nextLine();
                
                System.out.println("Enter Password");
                String password = scanner.nextLine();
                
                System.out.println("Enter Cellphone Number");
                String cellPhone = scanner.nextLine();
                
                String registrationMessage = userLogin.registerUser(username, password, cellPhone);
                System.out.println("n\" + registrationMessage");
                       
                if (registrationMessage.contains("incorrectly formatted") || registrationMessage.contains("not corretly formatted")) {
                    System.out.println("\nRegistration failed. Exiting" );
                    scanner.close();
